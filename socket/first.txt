粘包的几种处理方式：
第一种：固定长度（fix length）
    客户端在发送数据时，以固定长度切包，比如1024字节，并且在数据长度不够1024时，通过填充的方式补足1024,比如简单的文件传输
第二种：使用固定的分隔符（delimiter based）
    客户端在发送数据包时，在每个包的末尾使用固定的分隔符，比如使用回车换行符，一般文本协议使用比较多一些
第三种：消息头+消息体（length field based frame decoder）
    定义一个消息头，并且在消息头中保存整个消息的长度，读取时根据消息头中的消息长度来获取完整的消息，比如goim
